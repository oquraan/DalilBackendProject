# Copy this file to a new file named .env and write your own values.
# The .env file stays on your computer: never share it and never upload it.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=expense_tracker
